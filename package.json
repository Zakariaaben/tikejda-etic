{
  "name": "repartition-tikejda",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "@trpc/server": "^11.5.0",
      "@trpc/client": "^11.5.0",
      "better-auth": "^1.4.0",
      "dotenv": "^17.2.2"
    }
  },
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types",
    "dev:native": "bun run --filter native dev",
    "dev:web": "bun run --filter web dev",
    "db:push": "bun run --filter @repartition-tikejda/db db:push",
    "db:studio": "bun run --filter @repartition-tikejda/db db:studio",
    "db:generate": "bun run --filter @repartition-tikejda/db db:generate",
    "db:migrate": "bun run --filter @repartition-tikejda/db db:migrate"
  },
  "packageManager": "bun@1.3.2",
  "dependencies": {
    "dotenv": "^17.2.2",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^22.13.11"
  }
}
